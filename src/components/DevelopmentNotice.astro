---
// Entwicklungshinweis-Komponente
---

<div class="development-notice">
  <div class="notice-content">
    <div class="notice-icon">⚠️</div>
    <div class="notice-text">
      <h3>Website wird noch optimiert</h3>
      <p>Diese Seite befindet sich derzeit in der finalen Überarbeitung. Alle Inhalte sind noch nicht finalisiert, einige Funktionen werden noch optimiert und Bilder ausgetauscht.</p>
    </div>
    <button class="close-btn" onclick="closeDevelopmentNotice()">✕</button>
  </div>
</div>

<style>
  .development-notice {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    background: linear-gradient(135deg, var(--accent), #c4b6a5);
    border-bottom: 3px solid rgba(var(--gray-dark), 0.2);
    color: rgb(var(--gray-dark));
    padding: 15px 20px;
    z-index: 1000;
    box-shadow: var(--box-shadow);
    animation: slideDown 0.5s ease-out;
  }
  
  .notice-content {
    display: flex;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
    gap: 15px;
  }
  
  .notice-icon {
    font-size: 24px;
    animation: pulse 2s infinite;
    color: rgb(var(--gray-dark));
  }
  
  .notice-text {
    flex: 1;
  }
  
  .notice-text h3 {
    margin: 0 0 5px 0;
    font-size: 18px;
    font-weight: 600;
    font-family: "Inria Serif", serif;
    color: rgb(var(--gray-dark));
  }
  
  .notice-text p {
    margin: 0;
    font-size: 14px;
    opacity: 0.8;
    line-height: 1.4;
    color: rgb(var(--gray-dark));
  }
  
  .close-btn {
    background: none;
    border: 2px solid rgba(var(--gray-dark), 0.3);
    color: rgb(var(--gray-dark));
    font-size: 18px;
    cursor: pointer;
    padding: 8px;
    border-radius: 50%;
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    font-weight: bold;
  }
  
  .close-btn:hover {
    background-color: rgba(var(--gray-dark), 0.1);
    border-color: rgba(var(--gray-dark), 0.5);
    transform: scale(1.1);
  }
  
  @keyframes slideDown {
    from {
      transform: translateY(-100%);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }
  
  @keyframes pulse {
    0%, 100% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.2);
      filter: brightness(1.1);
    }
  }
  
  /* Responsive Design */
  @media (max-width: 768px) {
    .development-notice {
      padding: 12px 15px;
    }
    
    .notice-content {
      gap: 10px;
    }
    
    .notice-icon {
      font-size: 20px;
    }
    
    .notice-text h3 {
      font-size: 16px;
    }
    
    .notice-text p {
      font-size: 13px;
    }
    
    .close-btn {
      font-size: 18px;
      width: 26px;
      height: 26px;
    }
  }
  
  /* Verstecke die Anzeige wenn sie geschlossen wurde */
  .development-notice.hidden {
    display: none;
  }
  
  /* Körper-Anpassung um Platz für die fixierte Anzeige zu schaffen */
  :global(body.has-dev-notice) {
    padding-top: 80px;
  }
  
  @media (max-width: 768px) {
    :global(body.has-dev-notice) {
      padding-top: 70px;
    }
  }
</style>

<script>
  function closeDevelopmentNotice() {
    const notice = document.querySelector('.development-notice');
    const body = document.body;
    
    if (notice) {
      notice.classList.add('hidden');
      body.classList.remove('has-dev-notice');
      
      // Speichere im localStorage, dass der Nutzer die Anzeige geschlossen hat
      localStorage.setItem('devNoticeHidden', 'true');
    }
  }
  
  // Prüfe beim Laden der Seite, ob die Anzeige bereits geschlossen wurde
  document.addEventListener('DOMContentLoaded', function() {
    const isHidden = localStorage.getItem('devNoticeHidden');
    const notice = document.querySelector('.development-notice');
    const body = document.body;
    
    if (isHidden === 'true') {
      if (notice) {
        notice.classList.add('hidden');
      }
    } else {
      body.classList.add('has-dev-notice');
    }
  });
  
  // Stelle sicher, dass die Funktion global verfügbar ist
  (window as any).closeDevelopmentNotice = closeDevelopmentNotice;
</script>
